name: AquaGwada scheduled jobs disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: AquaGwada shutdown notice
        run: echo "AquaGwada has been stopped; scheduled jobs are disabled."
